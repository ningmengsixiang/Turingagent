{
  "name": "turing-agent",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r build && pnpm -r test",
    "dev:gateway": "pnpm --fail-if-no-match --filter @ta/gateway dev"
  }
}
