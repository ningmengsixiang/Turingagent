apiVersion: v1
kind: ConfigMap
metadata:
  name: ta-config
  namespace: ta-prod
data:
  NODE_ENV: "production"
  PORT: "3001"
  MODEL_PROVIDER: "deepseek"
  MODEL_NAME: "deepseek-chat"
  MINIO_ENDPOINT: "minio:9000"
  MINIO_BUCKET: "ta-files"
  POSTGRES_DB: "ta_prod"
  POSTGRES_USER: "ta"
