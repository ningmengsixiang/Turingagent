import type { FastifyInstance } from 'fastify'
import type { Config } from './config.js'

// Task 4 替换为完整实现（welcome + echo + 鉴权）
export function registerWs(app: FastifyInstance, _config: Config): void {
  void app
}
