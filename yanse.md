# Ta 桌面端黑白灰高级简约设计体系

参照 Manus 的设计美学：**极致克制、单色系、以黑白灰构建全部视觉层级，靠排版与留白传递品质感。**

---

## 一、设计哲学

Manus 的设计之所以高级，核心在于：

> **没有颜色可以依赖时，产品的质感完全来自灰度层次、字体排版、间距节奏和微妙的动效。**

Ta 采用同样的策略：

- **全部界面由黑、白、灰构成**，不使用任何彩色作为 UI 主色。
- **强调色仅在绝对必要时出现**（如危险操作、成功状态），且饱和度极低，几乎融入灰度体系。
- **层级靠灰度深浅区分**，而非边框或彩色。
- **动效极简**，只有淡入淡出和微小的位移动画。
- **字体是视觉的主角**，通过字重、字号、字距建立信息秩序。

---

## 二、色彩系统（黑白灰）

### 2.1 浅色模式（Light Mode）

#### 灰度色阶

```
┌─────────────────────────────────────────────────────────┐
│  Gray Scale（冷灰，微带蓝调，更显高级）                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  gray-50     #FAFAFB    全局背景（最浅）                │
│  gray-100    #F4F4F5    侧边栏背景                      │
│  gray-150    #ECECEE    hover 状态                      │
│  gray-200    #E4E4E7    分割线、描边（极浅）            │
│  gray-300    #D4D4D8    输入框描边                      │
│  gray-400    #A1A1AA    禁用文字、占位符                │
│  gray-500    #71717A    辅助文字                        │
│  gray-600    #52525B    次要文字                        │
│  gray-700    #3F3F46    正文次级                        │
│  gray-800    #27272A    正文主色（近似黑）              │
│  gray-900    #18181B    标题、强调文字（近黑）          │
│  gray-950    #0A0A0A    极强调、代码背景               │
│                                                         │
│  white       #FFFFFF    卡片、消息气泡、悬浮层          │
│  black       #000000    纯黑（极少使用）                │
└─────────────────────────────────────────────────────────┘
```

#### 背景层级

| 层级 | 颜色 | 用途 |
|------|------|------|
| 全局背景 | `#FAFAFB` | 主内容区 |
| 侧边栏背景 | `#F4F4F5` | 左侧导航，与主区域微妙区分 |
| 卡片/气泡 | `#FFFFFF` | 消息气泡、面板、弹窗 |
| 悬浮层 | `#FFFFFF` + 阴影 | 下拉菜单、Popover |
| 深色区域 | `#18181B` | 代码块、特殊强调面板 |

#### 文字层级

| 层级 | 颜色 | 字号/字重 | 用途 |
|------|------|-----------|------|
| 一级文字 | `#18181B` | 15px/600 | 群名称、卡片标题、用户昵称 |
| 二级文字 | `#27272A` | 14px/500 | 消息正文、按钮文字 |
| 三级文字 | `#52525B` | 13px/400 | 辅助说明、时间戳 |
| 四级文字 | `#71717A` | 12px/400 | 占位符、提示信息 |
| 禁用文字 | `#A1A1AA` | 14px/400 | 不可用状态 |

#### 分割线与描边

| 元素 | 颜色 | 说明 |
|------|------|------|
| 细分割线 | `#E4E4E7` | 列表分隔、时间分割线两侧 |
| 输入框描边 | `#D4D4D8` | 默认状态 |
| 聚焦描边 | `#18181B` | 聚焦时变黑，体现极简力量感 |
| 卡片描边 | `#ECECEE` 或无线框 | 极浅或仅靠阴影区分 |

#### 强调色（极克制地使用）

| 用途 | 颜色 | 说明 |
|------|------|------|
| 主强调（选中、在线状态） | `#18181B`（黑色） | 以黑色作为强调色 |
| 智能体标识 | `#52525B`（深灰） | AI 头像/名称标识用深灰，非彩色 |
| 成功状态 | `#3D3D3D` 或对勾 | 尽量融入灰度，不突兀 |
| 警告状态 | `#8A8A8A` 或警示图标 | 灰度化处理 |
| 危险操作 | `#B91C1C`（极暗红） | 仅删除/失败时出现，饱和度极低 |
| 未读提示 | `#18181B`（黑色实心点） | 代替传统红色/蓝色 |

> **核心原则**：黑色就是 Ta 的品牌色。所有强调、选中、活跃状态都用黑色或深灰表达。彩色在 Ta 的界面中几乎不存在。

### 2.2 深色模式（Dark Mode）

```
┌─────────────────────────────────────────────────────────┐
│  Dark Gray Scale                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  dark-bg      #0F0F10    全局背景                       │
│  dark-sidebar #151516    侧边栏背景                     │
│  dark-card    #1A1A1B    卡片、气泡                     │
│  dark-hover   #222224    hover 状态                     │
│  dark-line    #2A2A2C    分割线、描边                   │
│  dark-input   #262628    输入框背景                     │
│                                                         │
│  dark-ink-1   #F5F5F5    一级文字                       │
│  dark-ink-2   #D4D4D4    二级文字                       │
│  dark-ink-3   #A0A0A0    三级文字                       │
│  dark-ink-4   #606060    占位符                         │
│                                                         │
│  dark-accent  #F5F5F5    强调色（白色）                 │
│  dark-code    #0A0A0A    代码块背景                     │
└─────────────────────────────────────────────────────────┘
```

**深色模式关键点**：
- 背景不是纯黑，而是 `#0F0F10` 的极深灰，更柔和。
- 强调色反转为白色 `#F5F5F5`。
- 阴影在深色模式下几乎不可见，层级靠背景灰度深浅区分。
- 代码块背景比全局背景更深（`#0A0A0A`），形成内凹感。

---

## 三、布局与组件设计（黑白灰版）

### 3.1 整体布局

```
┌──────────────────────────────────────────────────────────────────────┐
│ 顶部栏（48px，背景 #FAFAFB，底部 1px #E4E4E7 分割线）                 │
│ 左侧：◯  Ta（黑色 Logo + 文字）         右侧：[搜索] [通知] [设置] [头像]│
├───────────┬──────────────────────────────┬─────────────────────────────┤
│ 左侧侧边栏 │ 中间主区域                     │ 右侧面板（可折叠）            │
│ 240px     │ 背景 #FAFAFB                 │ 280px，背景 #FFFFFF          │
│ 背景       │                              │ 左侧 1px #E4E4E7 分割线    │
│ #F4F4F5   │  群名称 + 状态                │                            │
│           │  ┌────────────────────────┐ │  项目进度                   │
│ 群聊列表  │  │ 消息流                  │ │  ▓▓▓▓▓░░ 65%              │
│ 项目列表  │  │                        │ │                            │
│           │  │ （白色气泡 + 黑色文字） │ │  交付物                     │
│           │  │                        │ │  [ ] 需求规格说明书          │
│           │  └────────────────────────┘ │  [ ] 架构文档               │
│           │  输入区（白色，顶部细分割线） │                            │
└───────────┴──────────────────────────────┴─────────────────────────────┘
```

### 3.2 侧边栏设计

```
┌─────────────────┐
│  [搜索🔍]       │ ← 背景 white，边框 #E4E4E7，圆角 8px
│                 │
│  项目            │ ← 12px，#71717A，字距 0.5px，全大写
│  ▎员工考勤系统   │ ← 选中：背景 #FFFFFF + 左侧 2px 黑色条
│   进销存管理     │ ← 未选：透明，hover 背景 #ECECEE
│   CRM 系统      │
│                 │
│  沟通群          │
│   需求讨论-考勤  │
│                 │
│  智能体          │
│  ● Ta-PM       │ ← 在线状态：黑色实心圆点（6px）
│  ● Ta-Architect │
│  ○ Ta-QA       │ ← 离线：空心灰圈
│                 │
│  [+ 新建项目]   │ ← 黑色实心按钮，白色文字，圆角 8px
└─────────────────┘
```

**设计细节**：
- 选中态：白色背景 + **左侧 2px 黑色竖条**（不是彩色），文字 `#18181B` 加粗。
- 未读消息：黑色小圆点（6px），不用彩色。
- 在线状态：黑色实心点 = 在线，灰色空心圈 = 离线，灰色实心点 = 忙碌。
- “新建项目”按钮：**黑色填充**，是全界面唯一的实心按钮，视觉焦点自然形成。
- hover：`#ECECEE`，过渡 150ms。

### 3.3 消息气泡设计

#### 人类用户消息

```
┌────────────────────────────────────────────────┐
│  [头像]  张三                    14:32        │
│          ┌──────────────────────────────────┐ │
│          │ 我们需要一个考勤系统，支持打卡。  │ │
│          │ 背景：white                      │ │
│          │ 边框：1px #E4E4E7               │ │
│          │ 文字：#27272A，14px             │ │
│          └──────────────────────────────────┘ │
└────────────────────────────────────────────────┘
```

#### 智能体消息

```
┌────────────────────────────────────────────────┐
│  [AI]  Ta-PM（深灰标识）         14:32       │
│       ┌──────────────────────────────────┐    │
│       │ 好的，我先确认几个关键信息：      │    │
│       │ 背景：#F4F4F5（浅灰）           │    │
│       │ 无边框或 1px #ECECEE            │    │
│       │ 文字：#27272A，14px             │    │
│       │ 左侧 2px 黑色细条（可选项）      │    │
│       └──────────────────────────────────┘    │
└────────────────────────────────────────────────┘
```

**气泡规范**：
- 人类消息：纯白底 + 极浅灰描边
- 智能体消息：浅灰底（`#F4F4F5`）+ 无描边，或左缘 2px 黑色细条增强识别
- 圆角：统一 12px，发送方对应角可 4px
- 内边距：12px 文字类，16px 卡片类
- 连续消息：同发送者连续消息，仅首条显示头像，间距 4px
- 时间分割线：“今天 14:32”，文字 12px `#A1A1AA`，两侧 1px `#E4E4E7` 细线

#### 消息状态
- 发送中：气泡 `opacity: 0.5`
- 发送成功：恢复正常
- 发送失败：气泡右侧出现极暗红色感叹号（`#B91C1C`）

### 3.4 输入区设计

```
┌─────────────────────────────────────────────────────────────┐
│  [图片] [文件] [语音] [@] [表情]           [格式] [更多]      │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 输入消息...                                           │ │
│  │ 背景：white，边框 1px #D4D4D8，圆角 8px               │ │
│  └───────────────────────────────────────────────────────┘ │
│                                              [发送 ⬆]      │
│                                    黑色圆形按钮，白色箭头    │
└─────────────────────────────────────────────────────────────┘
```

**关键细节**：
- 输入框默认：白底 + `#D4D4D8` 描边
- 聚焦时：描边变 **黑色**（`#18181B`）+ 无彩色光环（极简力量感）
- 发送按钮：黑色圆形（40px），白色向上箭头图标，hover 变 `#27272A`
- 工具栏图标：`#71717A`，hover 变 `#18181B`
- 语音输入时：输入框内显示声波动画，声波为黑色线条

### 3.5 卡片与弹窗

#### 确认卡片（消息流内）

```
┌────────────────────────────────┐
│                                 │
│ 需求确认                        │ ← 15px/600，#18181B
│                                 │
│ 《需求规格说明书 v0.1》         │ ← 14px/500，#27272A
│ 需求已澄清完毕，请确认或提出    │ ← 13px/400，#52525B
│ 修改意见。                      │
│                                 │
│ [查看文档 ↗]                   │ ← 13px/500，#18181B 下划线
│                                 │
│ ─────────────────────────────── │ ← 1px #E4E4E7
│                                 │
│ [确认]     [提出修改]           │ ← 确认：黑色填充白字
│                                 │   修改：白底黑字+灰描边
└────────────────────────────────┘
```

#### 弹窗

```
┌─────────────────────────────────┐
│  项目设置                    ✕  │ ← 16px/600
│                                  │
│  项目名称                        │
│  [员工考勤系统___________]       │ ← 白底，#D4D4D8 描边
│                                  │
│  项目类型                        │
│  [下拉选择____________▾]        │
│                                  │
│  ─────────────────────────────   │
│  [取消]        [创建项目]       │ ← 创建：黑底白字
└─────────────────────────────────┘
```

- 弹窗圆角 16px，阴影 `0 8px 32px rgba(0,0,0,0.12)`
- 遮罩：`rgba(0,0,0,0.35)`，无模糊或极轻微
- 主按钮始终黑色填充，次按钮白底 + 灰描边 + 黑字

### 3.6 右侧面板

```
┌──────────────────┐
│ 项目信息         │ ← 12px/600，#71717A，全大写，字距 0.5px
│                  │
│ 员工考勤系统     │ ← 15px/600
│ 开发中           │ ← 13px，#52525B，前面黑色小圆点
│                  │
│ ─────────────────│ ← #E4E4E7
│ 进度             │
│ ▓▓▓▓▓▓▓░░░ 65%  │ ← 进度条：黑色填充，灰色轨道
│                  │
│ ─────────────────│
│ 交付物           │
│ [📄] 需求规格书  │ ← 文件图标灰度处理
│ [📄] 架构文档    │
│ [🎨] UI原型     │
│                  │
│ ─────────────────│
│ 任务             │
│ ✓ 需求澄清       │ ← 完成：灰色勾
│ ◉ 架构设计       │ ← 进行中：黑色填充圆
│ ○ UI设计         │ ← 待开始：空心灰圈
│ ○ 开发           │
│                  │
│ ─────────────────│
│ 记忆文档         │
│ [📄] 需求澄清记录 │
│ [📄] 决策日志    │
└──────────────────┘
```

- 进度条：轨道 `#ECECEE`，填充黑色 `#18181B`，高度 4px，圆角 2px
- 任务状态图标：全部用黑白灰表达，不用彩色
  - 完成：`✓` 灰色 `#A1A1AA`
  - 进行中：黑色实心圆
  - 待开始：灰色空心圆

### 3.7 智能体标识

智能体在群聊中需要被清晰识别，但又要保持黑白灰风格：

```
人类用户：
[头像] 张三     ← 头像：用户上传的图片
       市场部    ← 13px，#A1A1AA

智能体：
[AI]  Ta-PM    ← 头像：黑色圆角方块，内含白色角色图标
      产品经理  ← 13px，#A1A1AA
```

- 智能体头像：**黑色背景圆角方块**（8px），内含白色简洁图标（如 PM 用文档图标、Architect 用结构图标）
- 智能体名称后跟一个小灰标“AI”（白字灰底，12px，圆角 4px）
- 用户头像不做特殊处理

---

## 四、动效规范（黑白灰版）

| 场景 | 动效 | 时长 | 缓动 |
|------|------|------|------|
| 消息进入 | 淡入 + 上移 4px | 200ms | ease-out |
| 正在输入 | 三个灰点依次跳动 | 循环，各 200ms 间隔 | ease-in-out |
| 按钮 hover | 背景灰度加深 | 150ms | ease-out |
| 列表 hover | 背景变 `#ECECEE` | 150ms | ease-out |
| 侧边栏选中 | 背景变白 + 黑色条滑入 | 200ms | ease-out |
| 弹窗打开 | 淡入 + scale(0.97→1) | 200ms | ease-out |
| 面板展开/折叠 | 宽度变化 | 250ms | ease-in-out |
| 进度条更新 | 填充平滑过渡 | 300ms | ease-out |
| 发送按钮 | 箭头图标轻微上移 | 150ms | ease-out |

**原则**：动效看不见才是最好的动效。所有动画都应该是用户“感觉到”而非“注意到”的。

---

## 五、Tailwind 配置（完整实现）

```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        // 灰度体系（冷灰）
        gray: {
          50: '#FAFAFB',
          100: '#F4F4F5',
          150: '#ECECEE',
          200: '#E4E4E7',
          300: '#D4D4D8',
          400: '#A1A1AA',
          500: '#71717A',
          600: '#52525B',
          700: '#3F3F46',
          800: '#27272A',
          900: '#18181B',
          950: '#0A0A0A',
        },
        // 语义色（极克制）
        danger: {
          DEFAULT: '#B91C1C',
          subtle: '#FEF2F2',
        },
        success: {
          DEFAULT: '#3D3D3D',
          subtle: '#F4F4F5',
        },
        warning: {
          DEFAULT: '#8A8A8A',
          subtle: '#F9F9FA',
        },
      },
      fontFamily: {
        sans: [
          'Inter',
          'SF Pro Display',
          'Segoe UI',
          'HarmonyOS Sans',
          'PingFang SC',
          'Microsoft YaHei',
          'sans-serif',
        ],
        mono: [
          'JetBrains Mono',
          'SF Mono',
          'Consolas',
          'monospace',
        ],
      },
      borderRadius: {
        xs: '4px',
        sm: '6px',
        md: '8px',
        lg: '12px',
        xl: '16px',
        bubble: '12px',
        full: '9999px',
      },
      boxShadow: {
        'float-sm': '0 1px 3px rgba(0,0,0,0.04)',
        'float-md': '0 4px 12px rgba(0,0,0,0.06)',
        'float-lg': '0 8px 32px rgba(0,0,0,0.10)',
        'overlay': '0 16px 48px rgba(0,0,0,0.15)',
        'none': 'none',
      },
      letterSpacing: {
        'section': '0.08em',
      },
      transitionTimingFunction: {
        'out-soft': 'cubic-bezier(0.33, 1, 0.68, 1)',
        'in-out-soft': 'cubic-bezier(0.65, 0, 0.35, 1)',
      },
      transitionDuration: {
        'fast': '100ms',
        'normal': '150ms',
        'slow': '200ms',
        'slower': '300ms',
      },
      spacing: {
        '4.5': '18px',
        '13': '52px',
        '15': '60px',
      },
    },
  },
  plugins: [],
}

export default config
```

---

## 六、核心组件样式示例（shadcn/ui 定制）

基于 shadcn/ui 的组件进行黑白灰定制：

### 6.1 Button 按钮

```typescript
// 主按钮（黑色实心）
'bg-gray-900 text-white hover:bg-gray-800 active:bg-gray-950 transition-colors duration-150 rounded-md font-medium text-sm'

// 次按钮（白底描边）
'bg-white text-gray-900 border border-gray-300 hover:bg-gray-50 hover:border-gray-400 active:bg-gray-100 transition-colors duration-150 rounded-md font-medium text-sm'

// 幽灵按钮（无边框无背景）
'bg-transparent text-gray-600 hover:text-gray-900 hover:bg-gray-150 transition-colors duration-150 rounded-md font-medium text-sm'

// 危险按钮
'bg-white text-danger border border-danger/30 hover:bg-danger-subtle transition-colors duration-150 rounded-md font-medium text-sm'
```

### 6.2 Input 输入框

```typescript
// 默认
'bg-white border border-gray-300 rounded-md px-3 py-2 text-sm text-gray-800 placeholder:text-gray-400 focus:outline-none focus:border-gray-900 transition-colors duration-150'

// 聚焦时：边框从灰色过渡到黑色，无彩色光环
```

### 6.3 侧边栏导航项

```typescript
// 默认
'px-3 py-2 rounded-md text-gray-600 hover:bg-gray-150 hover:text-gray-900 transition-colors duration-150 cursor-pointer text-sm'

// 选中
'px-3 py-2 rounded-md bg-white text-gray-900 font-medium shadow-float-sm relative before:absolute before:left-0 before:top-1/2 before:-translate-y-1/2 before:w-0.5 before:h-4 before:bg-gray-900 before:rounded-full transition-colors duration-150 cursor-pointer text-sm'
```

### 6.4 消息气泡

```typescript
// 人类用户消息
'bg-white border border-gray-200 rounded-bubble px-3.5 py-2.5 max-w-[70%] shadow-float-sm'

// 智能体消息
'bg-gray-100 rounded-bubble px-3.5 py-2.5 max-w-[70%]'

// 连续消息（同一发送者）
'mt-1'
// 非连续消息（不同发送者）
'mt-4'
```

### 6.5 确认卡片

```typescript
// 容器
'bg-white rounded-xl border border-gray-200 shadow-float-md p-4 max-w-[420px]'

// 标题
'text-[15px] font-semibold text-gray-900 mb-1'

// 正文
'text-sm text-gray-600 mb-3'

// 分割线
'border-t border-gray-200 my-3'

// 操作区
'flex gap-2'
```

---

## 七、UI 组件库选型更新

基于黑白灰设计要求，推荐组合：

| 层次 | 推荐 | 理由 |
|------|------|------|
| 组件基础 | **shadcn/ui**（Radix UI + Tailwind） | 完全可控，易于实现黑白灰定制 |
| 动效 | **Framer Motion** | 实现细腻的淡入和位移 |
| 图标 | **lucide-react** | 线性图标，天然黑白灰风格 |
| 字体 | **Inter**（全局）+ **JetBrains Mono**（代码） | 现代、中性、专业 |
| 深色模式 | `next-themes` + Tailwind `dark:` | 类切换，灵活控制 |

**不需要**：任何带主题色的组件库（如 Ant Design 默认蓝色主题），因为它们默认的彩色风格与黑白灰美学冲突。

---

## 八、界面效果参考

### 8.1 登录/启动页

```
┌─────────────────────────────────────────┐
│                                         │
│                                         │
│              [Ta Logo：黑色方块]         │
│                                         │
│           Turing Agent                  │
│        你的软件开发工厂                  │ ← 15px，#71717A
│                                         │
│      ┌─────────────────────┐            │
│      │  work@company.com   │            │ ← 白底，灰描边
│      └─────────────────────┘            │
│      ┌─────────────────────┐            │
│      │  ***********        │            │
│      └─────────────────────┘            │
│                                         │
│      ┌─────────────────────┐            │
│      │      登  录         │            │ ← 黑底白字
│      └─────────────────────┘            │
│                                         │
│       忘记密码？  |  企业 SSO            │
│                                         │
└─────────────────────────────────────────┘
```

极致简约，只有黑白灰，黑色登录按钮是唯一视觉焦点。

### 8.2 群聊界面

左侧浅灰侧边栏，中间大面积的近白背景上流动着白色和浅灰色气泡，黑色文字清晰克制，只有“发送”按钮和选中态是纯黑色，形成最自然的视觉焦点。智能体的 AI 标识是深灰色，不抢眼但足够辨识。

整界面没有一丁点彩色，却因为灰度层次的精准控制而充满高级感。

---

## 九、总结

Ta 的黑白灰设计体系，完全参照 Manus 的极简哲学：

1. **黑色是唯一的品牌色**：黑色实心按钮、黑色选中条、黑色在线点，在灰度世界中自然地成为视觉焦点。
2. **灰度有精确的层级**：从 `#FAFAFB` 到 `#0A0A0A`，每一级灰度都有明确的用途，不多不少。
3. **字体和排版是主角**：Inter 字体的不同字重和字号，配合精心设计的间距，构建出清晰的信息秩序。
4. **动效不可见**：淡入淡出、150-200ms 的过渡，用户感觉到流畅但不会注意到动画存在。
5. **彩色几乎为零**：仅在危险操作时出现极暗红色，其余全部由黑白灰完成表达。

**这就是 Ta 的高级感——不是靠颜色堆砌，而是靠克制与精准。**

---

## 十、实现位置（2026-08-13 补充）

- 实现：`prototype/`（Next.js + shadcn/ui + Framer Motion + lucide-react + next-themes + Tailwind v4）。
- 本文档 §五 的 `tailwind.config.ts` 为 v3 写法；实现时按 Tailwind v4 的 `@theme` 语法等价落地（`prototype/src/app/globals.css`），令牌值一一对应（含 gray-150、radius-bubble、shadow-float-*、transition 时长与缓动）。
- 黑白灰规则在实现中的硬性落地：无彩色主色；未读/在线/选中一律黑色表达；危险仅 `#B91C1C`；智能体 = 黑色圆角方块头像 + 灰底「AI」标；确认按钮黑底白字；聚焦描边变黑、无彩色光环。
- 原静态原型保留在 `prototype-static/` 作为对照。

---

## 十一、极简白方向调整（2026-08-13 用户确认）

在黑白灰体系基础上进一步收向**极简白**，以下规则优先级高于前文：

1. **白色为主**：全局背景改纯白 `#FFFFFF`（原 #FAFAFB）；灰色仅作次级填充（#F7F7F8）与细线（#EBEBEE）。
2. **扁平化**：取消全部阴影（shadow-float-* 令牌置空）；头像取消渐变，统一浅灰底灰字；卡片/气泡靠 1px 细线与底色层级区分，无悬浮抬升。
3. **大面积留白**：消息流左右留白增大（px-10），消息间距、面板内边距加大，白色区域本身就是层级。
4. **分区明确**：左侧导航、消息流、右侧面板以 hairline 分割线划分；组标签（uppercase 字距）继续承担分区提示。
5. **排版规整**：标题 15/600、正文 14/400、辅助 13、元信息 11 等宽——四级字号不动摇；行高统一 relaxed。
6. **按钮简洁直观**：主操作仍为黑实心（唯一实心），次操作白底细描边，图标按钮 ghost；悬停仅变色不变形。
7. **交互易识别**：可点击元素必有 hover 态与 focus-visible 黑环；黑色仍承担全部强调语义（选中条、未读点、在线点）。